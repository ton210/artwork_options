<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%- include('../partials/favicon') %>
  <%- include('../partials/analytics') %>
  <title><%= title %></title>
  <meta name="description" content="<%= meta.description %>">
  <meta name="keywords" content="<%= meta.keywords %>">
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <style>body { font-family: 'Inter', sans-serif; }</style>

  <!-- FAQ Schema -->
  <script type="application/ld+json"><%- JSON.stringify(faqSchema) %></script>
</head>
<body class="bg-gray-50">
  <%- include('../partials/header') %>

  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <!-- Breadcrumb -->
    <nav class="mb-6 text-sm text-gray-600">
      <a href="/" class="hover:text-green-600">Home</a>
      <span class="mx-2">/</span>
      <a href="/dispensaries/<%= state.slug %>" class="hover:text-green-600"><%= state.name %></a>
      <span class="mx-2">/</span>
      <span class="text-gray-900">FAQ</span>
    </nav>

    <h1 class="text-4xl font-bold text-gray-900 mb-4"><%= state.name %> Dispensary FAQ</h1>
    <p class="text-xl text-gray-600 mb-8">Your questions about cannabis dispensaries in <%= state.name %> answered</p>

    <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-8">
      <div class="flex items-start gap-3">
        <div class="text-2xl">💡</div>
        <div>
          <h3 class="font-bold text-gray-900 mb-2">Quick Links</h3>
          <div class="flex flex-wrap gap-3">
            <a href="/dispensaries/<%= state.slug %>" class="text-blue-600 hover:underline">Browse <%= state.name %> Dispensaries →</a>
            <a href="/blog" class="text-blue-600 hover:underline">Read Our Guides →</a>
          </div>
        </div>
      </div>
    </div>

    <div class="space-y-6">
      <% faqs.forEach((faq, index) => { %>
        <div class="bg-white rounded-lg shadow-md p-6">
          <h2 class="text-xl font-bold text-gray-900 mb-3"><%= faq.question %></h2>
          <p class="text-gray-700 leading-relaxed"><%= faq.answer %></p>
        </div>
      <% }); %>
    </div>

    <div class="mt-12 bg-green-50 border border-green-200 rounded-lg p-8 text-center">
      <h3 class="text-2xl font-bold mb-4">Find Dispensaries in <%= state.name %></h3>
      <p class="text-gray-700 mb-6">Browse our complete rankings and find the perfect dispensary for your needs.</p>
      <a href="/dispensaries/<%= state.slug %>" class="inline-block bg-green-600 text-white px-8 py-3 rounded-lg hover:bg-green-700 transition font-semibold">
        View <%= state.name %> Dispensaries →
      </a>
    </div>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>
